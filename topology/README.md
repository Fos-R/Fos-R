# Topologies

Default topologies have been generated with [FedITN_Gen](https://gitlab.inria.fr/sotern/topology-generator). The configuration files used to create them are included in the `config_files` directory. The generated topologies have been converted to the Fos-R network format with `convert_topology.py`.
